import { beforeEach, describe, expect, it, vi } from "vitest";

describe.todo("amplitude [channel]", () => {
  beforeEach(() => {
    //
  });

  it("should install channel", () => {
    //
  });

  it("should send track event", () => {
    //
  });

  it("should send page event", () => {
    //
  });

  it("should send identify event", () => {
    //
  });

  it("should uninstall channel", () => {
    //
  });
});
