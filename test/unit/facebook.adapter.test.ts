import { beforeEach, describe, expect, it, vi } from "vitest";

describe.todo("facebook [adapter]", () => {
  beforeEach(() => {
    //
  });

  it("should only apply to facebook channel", () => {
    //
  });

  it("should rename (0-n) event name", () => {
    //
  });

  it("should change (0-n) event props", () => {
    //
  });
});
