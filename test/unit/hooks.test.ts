import { beforeEach, describe, expect, it, vi } from "vitest";

describe.todo("hooks", () => {
  beforeEach(() => {
    //
  });

  it("should be able to rename all event via hook", () => {
    //
  });

  it("should be able to change all event props via hook", () => {
    //
  });

  it("should abort all event via hook", () => {
    //
  });

  it("should be able to channel event via hook", () => {
    //
  });

  it("should be able to channel event props via hook", () => {
    //
  });

  it("should abort channel event via hook", () => {
    //
  });

  it("should call hook after all events", () => {
    //
  });

  it("should call hook after channel event", () => {
    //
  });
});
